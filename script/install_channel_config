#!/usr/bin/env bash
#
# The local-first fork does not install Warp's private channel-config binary.
# Default local development falls back to the OSS channel configuration.

echo "Skipping private Warp channel config installation in the local-first fork."
exit 0
